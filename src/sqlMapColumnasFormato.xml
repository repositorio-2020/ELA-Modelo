<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Formatos">

  <!-- Use type aliases to avoid typing the full classname every time. -->
  <typeAlias alias="ColumnasFormato" type="isahc.admin.paramtos.ColumnasFormato"/>

  <!-- Result maps describe the mapping between the columns returned
       from a query, and the class properties.  A result map isn't
       necessary if the columns (or aliases) match to the properties 
       exactly. -->
  <resultMap id="ColumnasFormatoResult" class="isahc.admin.paramtos.ColumnasFormato">
  </resultMap>

  <!-- Select with no parameters using the result map for Account class. -->
  <select id="selectAllColumnasFormato" resultMap="ColumnasFormatoResult">
    select * from cuf_detc_columnas
  </select>

  <!-- A simpler select example without the result map.  Note the 
       aliases to match the properties of the target result class. -->
  <select id="selectColumnasFormatoById" parameterClass="String" resultClass="isahc.admin.paramtos.ColumnasFormato">
     SELECT cuf_detc_secue,   
         cuf_for_secue,   
         cuf_par_tipval,   
         cuf_par_tipdat,   
         cuf_gran_secue,   
         cuf_detc_nombre,   
         cuf_detc_descri,   
         cuf_detc_columna,   
         cuf_detc_fila,   
         cuf_detc_long,   
         cuf_detc_decimal,   
         cuf_detc_obliga,   
         cuf_detc_hoja,   
         cuf_detc_codigo  
    FROM cuf_detc_columnas   
    WHERE cuf_detc_secue = #id#
         
  </select>
   
  <!-- Insert example, using the Account parameter class 
  <insert id="insertAccount" parameterClass="Formato">
    insert into ACCOUNT (
      ACC_ID,
      ACC_FIRST_NAME,
      ACC_LAST_NAME,
      ACC_EMAIL
    values (
      #id#, #firstName#, #lastName#, #emailAddress#
    )
  </insert>
-->
  
  
  <!-- Update example, using the Account parameter class 
  <update id="updateAccount" parameterClass="Formato">
    update ACCOUNT set
      ACC_FIRST_NAME = #firstName#,
      ACC_LAST_NAME = #lastName#,
      ACC_EMAIL = #emailAddress#
    where
      ACC_ID = #id#
  </update>
-->


  <!-- Delete example, using an integer as the parameter class -->
  <delete id="deleteColumnasFormatoById" parameterClass="int">
    delete from cuf_detc_columnas WHERE cuf_detc_secue = #id#
  </delete>

</sqlMap>